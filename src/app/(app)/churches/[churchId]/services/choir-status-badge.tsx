'use client'

// Phase D: ChoirStatusBadge removed — choir requirements are now managed
// via service role slots. This file is kept as an empty export for import compatibility.

export function ChoirStatusBadge(_props: {
  serviceId: string
  churchId: string
  userRole: string
}) {
  return null
}
